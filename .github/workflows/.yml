- name: Install dependencies  
  run: pip install python-telegram-bot  
``` |
| **Token Telegram atau Chat ID kosong** | Pastikan kamu sudah membuat secret di GitHub:  
- **TELEGRAM_TOKEN**  
- **TELEGRAM_CHAT_ID**  
Di workflow, gunakan:  
```yaml  
env:  
  TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}  
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}  
``` |
| **Kesalahan sintaksis di kode** | Periksa file `app.py`:  
- Apakah ada typo?  
- Apakah semua kutipan ditutup dengan benar?  
- Apakah token Telegram sudah diambil dari environment? |

#### 4. **Perbaiki dan Ulangi**
Setelah menemukan masalah, lakukan perubahan:
- Edit file `app.py` atau `workflow.yml`.
- Commit perubahan.
- Lalu jalankan ulang workflow.

---

### 📝 **Contoh Workflow Lengkap**

Berikut adalah versi lengkap `workflow.yml` yang bisa kamu coba:

```yaml
# .github/workflows/airdrop-bot.yml
name: Run CuanWatcher Bot

on:
  schedule:
    - cron: '0 * * * *'  # Jalankan tiap jam (pada menit ke-0)
  workflow_dispatch:     # Bisa juga dijalankan manual

jobs:
  send-airdrop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install python-telegram-bot

      - name: Run bot
        run: python app.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
